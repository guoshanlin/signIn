export const userData = state => state.userData

export const isLogin = state => state.isLogin

export const draftActivity = state => state.draftActivity

export const memberId = state => state.memberId
